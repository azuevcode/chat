import { makeStyles } from '@material-ui/core/styles';

export const useAppStyles = makeStyles({
    container: {
        maxWidth: 800,
        width: '100%',
        margin: 'auto',
    },
});

